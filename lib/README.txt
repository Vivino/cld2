To recompile this, run `go generate` in parent directory.

This can be needed if you experience errors like:

plugin.Open: /usr/lib/x86_64-linux-gnu/libstdc++.so.6: version `GLIBCXX_3.4.20' not found (required by /gopath/src/github.com/klauspost/cld2/lib/cld2go.so)

